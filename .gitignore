node_modules
dist
.env
.env.local
.DS_Store

# Contact sheet and its thumbnails. Regenerate with `npm run sheet`.
# Stripped from the build by the postbuild step as well.
public/dev

# brand-assets/ is committed deliberately: the logo vectors and the carbon
# texture total under 600KB and they are the only copies we have. The Exley
# build gitignores assets/ because it holds 1.4GB of photo originals — that
# reasoning does not apply here. When the client's photographs arrive, put the
# large originals in assets/ and ignore that instead.
