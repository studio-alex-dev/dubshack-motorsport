{
  "name": "dubshack-motorsport",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pages": "node scripts/build-pages.mjs",
    "dev": "npm run pages && vite",
    "build": "npm run pages && vite build",
    "preview": "vite preview",
    "postbuild": "rm -rf dist/dev",
    "images": "node scripts/build-images.mjs",
    "sheet": "node scripts/build-sheet.mjs",
    "test:form": "node scripts/test-enquiry-function.mjs",
    "test": "npm run test:form",
    "icons": "node scripts/build-icons.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
